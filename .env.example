# 連接埠（預設 3000）
PORT=3000
# JWT 簽章金鑰（正式環境務必改成隨機長字串）
JWT_SECRET=change-me-to-a-long-random-string
# 資料存放目錄（DB 與上傳附件，預設 ./data）
DATA_DIR=./data
# 正式部署設為 production（啟用 secure cookie，需 HTTPS）
NODE_ENV=production
